-- Shared between prototypes/fluid-pmr.lua (data stage) and scripts/fluid-pmr.lua
-- (control stage) so the fluid list only needs to change in one place.
-- Placeholder selection — swap freely, nothing depends on these specific names.

return {
    "molten-iron",
    "molten-copper",
    "lava",
    "ammonia",
    "fluorine",
}
